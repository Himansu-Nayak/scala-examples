package com.org.scala

/**
  * Scala is a expression language and not statement language.
  */
object ScalaExpression extends App{

  val exp = if( 2 == 3) false else true

  println("Scala If-Else is a expression and not a statement(java). It return a value" + exp)

  println("Method is associated with object in scala where is function is not. You need obj to call a method")

  println("Any is the super class of all class. Similar to Object in java")

  println("Default package available in scala are java.lang, scala package and scala.PreDef (utility pkg of scala)")

  println("Scala has just one operator i.e. = . Everything else are method")

  println("Scala default scope is public")

  println("Scala do not support static")



}
